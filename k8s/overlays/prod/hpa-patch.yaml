# ============================================
# Production HPA Patches
# More aggressive scaling for production
# ============================================

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ogscout-hpa
spec:
  minReplicas: 1
  maxReplicas: 3
